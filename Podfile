target 'VenuesNearby' do
  platform :ios, '8.2'
  use_frameworks!

  # Pods for VenuesNearby
  pod 'AFNetworking', '~> 3.0'

  target 'VenuesNearbyTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Quick'
    pod 'Nimble', :inhibit_warnings => true
  end

end
